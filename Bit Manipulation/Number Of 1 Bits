# Programming > Bit Manipulation > Number Of 1 Bits

public class Solution {
	public int numSetBits(long a) {
	    int count = 0;
	    while (a > 0) {
	        count += a%2;
	        a /= 2;
	    }
	    return count;
	}
}
